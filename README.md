# gopack
A minimalist, high-performance Windows installer builder written in Go. Uses binary fusion to merge executable logic with app payload into a single, UAC-free setup.exe. No complex Pascal or XML scripting required—configure everything instantly via JSON. Features auto-uninstall generation, desktop shortcuts, and seamless registry cleanup.
